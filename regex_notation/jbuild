(library
  ((name regex_notation)
   (public_name regex_notation)
   (preprocess (pps (ppxlib.metaquot)))
   (libraries (ocaml-migrate-parsetree
               ppxlib.metaquot
               relit_helper))))

(ocamllex (lexer))
(ocamllex (absurd_lexer))

(menhir
 ((modules (parser absurd_parser))))

