(library
  ((name regex_example)
   (public_name regex_example)
   (preprocess (pps (ppxlib.metaquot)))
   (libraries (ocaml-migrate-parsetree
               ppxlib.metaquot
               relit_helper))))

(ocamllex (lexer))

(menhir
 ((modules (parser))))

