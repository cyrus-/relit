(library
  ((name regex_parser)
   (public_name regex_parser)
   (preprocess (pps (ppxlib.metaquot)))
   (libraries (ocaml-migrate-parsetree
               ppxlib.metaquot
               relit_helper))))

(ocamllex (lexer))

(menhir
 ((modules (parser))))

