(library
  ((name test_example)
   (public_name test_example)
   (preprocess (pps (ppxlib.metaquot)))
   (libraries (ocaml-migrate-parsetree
               ppxlib.metaquot
               relit_helper
               extlib))))
