(library
  ((name relit_helper)
   (public_name relit_helper)
   (preprocess (pps (ppxlib.metaquot)))
   (libraries (reason
               extlib
               base64
               ocaml-migrate-parsetree
               ppxlib.metaquot))))

